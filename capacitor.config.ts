import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.cookieapp.app", // TODO: Change this to your unique Bundle ID
  appName: "Cookie App",
  webDir: "dist",
};

export default config;
